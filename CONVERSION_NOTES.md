# HTML to JS conversion

- Extracted inline JavaScript from `index.html` into `app.js`.
- Updated `index.html` to load `app.js` as an external script.
- Kept markup, styles, assets, and external libraries unchanged.
